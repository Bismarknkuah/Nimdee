# Public URL of the API (Railway service URL + /api/v1)
NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
# Root domain of the platform. School portals live at <slug>.<ROOT_DOMAIN>. Use "localhost:3000" in development.
NEXT_PUBLIC_ROOT_DOMAIN=localhost:3000
